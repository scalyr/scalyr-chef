{
  // Log files to upload to Scalyr. You can use '*' wildcards here.
  // { path: "/var/log/httpd/access.log", attributes: {parser: "accessLog"} }
  logs: <%= Chef::JSONCompat.to_json_pretty(@logs) %>
}
